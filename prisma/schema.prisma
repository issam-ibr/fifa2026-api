// Copie du schema principal pour Render
// Pointer vers packages/database/prisma/schema.prisma en prod

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
